{
  "name": "graa",
  "version": "0.0.0",
  "author": "Fabian Meyer",
  "license": "MIT",
  "private": true,
  "main": "dist/index.js",
  "type": "module",
  "files": [
    "dist"
  ],
  "scripts": {
    "clean": "rimraf dist",
    "build": "npm run clean && tsc",
    "start": "node dist/index.js",
    "lint": "tsc --noEmit && eslint --ignore-path .gitignore .",
    "lint-fix": "tsc --noEmit && eslint --fix --ignore-path .gitignore ."
  },
  "devDependencies": {
    "@meyfa/eslint-config": "2.0.2",
    "@types/node": "17.0.25",
    "eslint": "8.23.0",
    "rimraf": "3.0.2",
    "typescript": "4.8.3"
  },
  "dependencies": {
    "@octokit/core": "4.0.5",
    "@octokit/plugin-paginate-rest": "4.2.2",
    "@octokit/plugin-rest-endpoint-methods": "6.5.0",
    "@octokit/types": "7.3.0",
    "superstruct": "0.16.0",
    "yaml": "2.1.1"
  }
}
